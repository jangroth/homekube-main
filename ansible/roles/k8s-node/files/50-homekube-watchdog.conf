# Vendor drop-in (40-rpi-enable-watchdog.conf) sets 1min, which pod-churn load
# stalls can trip. Safe to raise: sysfs max_timeout=0, so the kernel pets the
# BCM2835 in software past its short hw window rather than the chip itself
# enforcing 10min.
[Manager]
RuntimeWatchdogSec=10min

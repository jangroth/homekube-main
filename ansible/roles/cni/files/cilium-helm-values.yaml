---
# https://docs.cilium.io/en/stable/helm-reference/

upgradeCompatibility: "1.17"

k8sServiceHost: "10.0.0.20"   # switch IP — Tailscale is invisible to k8s
k8sServicePort: "6443"
kubeProxyReplacement: true     # kubeadm skips kube-proxy (skipPhases: addon/kube-proxy)

devices: "eth0,wlan0"          # eth0 for cluster traffic; wlan0 for MetalLB L2 (192.168.86.x) — see DECISION-030
routingMode: "tunnel"
tunnelProtocol: "vxlan"

ipam:
  mode: "kubernetes"

hubble:
  enabled: true
  relay:
    enabled: true
  ui:
    enabled: true
